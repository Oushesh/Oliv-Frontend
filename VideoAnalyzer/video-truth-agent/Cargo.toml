[package]
name = "video-truth-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
# Async Runtime
tokio = {version="1.0", features = ["full"]}

# API and Networking
reqwest = {version="0.12",features=["json"]}
serde = {version="1.0",features=["derive"]}
serde_json = "1.0"

